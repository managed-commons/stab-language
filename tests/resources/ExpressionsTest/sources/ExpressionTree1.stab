using java.lang;
using stab.query;
using stab.tree;

public class ExpressionTree1 {
    public static String test() {
        ExpressionTree<FunctionIntInt> expr = p => p;
        return ((ParameterExpression)((ExpressionStatement)expr.Body).Expression).Name;
    }
}